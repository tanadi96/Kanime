/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.myanimelist.net',"lh3.googleusercontent.com","avatars.githubusercontent.com"],  // Add this line
      },
};

export default nextConfig;
